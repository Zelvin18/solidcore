# Copy to .env.local for local development and set the real values.
# In production, set these in Vercel → Project → Settings → Environment Variables.

# Resend API key — used by /api/quote to email quotation requests to the team.
# Get one at https://resend.com (the sending domain solidcoreug.com must be verified there).
RESEND_API_KEY=
